fastapi>=0.104.1
uvicorn[standard]>=0.24.0
sqlalchemy>=2.0.23
alembic>=1.13.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.2
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0
python-dotenv==1.0.0
sentry-sdk[fastapi]>=2.20.0
prometheus-client>=0.21.0

# Database (optional for production)
# psycopg2-binary==2.9.9  # Uncomment for PostgreSQL support

# Security - Phase 2
slowapi>=0.1.9  # Rate limiting
pyotp>=2.9.0  # TOTP/2FA
qrcode>=7.4.2  # QR code generation

# Phase 5: Performance - Optional for development (requires Redis)
# redis>=4.5.0  # Uncomment if Redis is available
# celery>=5.3.0  # Uncomment if Redis is available

# Image processing - Phase 5
pillow>=10.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
faker>=20.0.0
